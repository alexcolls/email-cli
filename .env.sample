# Email Sending Configuration
# Copy this file to ~/.config/tempmail/.env and fill in your details

# SMTP Server Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Your Email Credentials
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here

# Display Name (shown in "From" field)
FROM_NAME=Your Name

# ============================================
# EASY SETUP OPTIONS (Choose one):
# ============================================

# Option 1: Gmail (FREE - Easiest)
# 1. Go to: https://myaccount.google.com/apppasswords
# 2. Generate an App Password
# 3. Use: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587
# 4. SMTP_USER=your-gmail@gmail.com
# 5. SMTP_PASS=the-16-char-app-password

# Option 2: Outlook/Hotmail (FREE)
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password

# Option 3: SendGrid (FREE - 100 emails/day)
# 1. Sign up at: https://sendgrid.com
# 2. Create API Key
# 3. Use: SMTP_HOST=smtp.sendgrid.net, SMTP_PORT=587
# 4. SMTP_USER=apikey
# 5. SMTP_PASS=your-sendgrid-api-key

# Option 4: Mailgun (FREE - 5,000 emails/month)
# 1. Sign up at: https://www.mailgun.com
# 2. Get SMTP credentials from dashboard
# 3. SMTP_HOST=smtp.mailgun.org
# 4. SMTP_PORT=587
